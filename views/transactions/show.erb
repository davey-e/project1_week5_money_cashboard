<% amount = Transaction.amount_pence_to_pounds(@transaction.amount) %>
<% tag = @transaction.tag() %>
<% vendor = @transaction.vendor() %>
<div id="plaintable">
<h2>Transaction Details</h2>
<table>
  <tbody>
    <tr>
      <td>Amount: </td>
      <td>£<%= amount %></td>
    </tr>
    <tr>
      <td>Tag: </td>
      <td><%= tag.name %></td>
    </tr>
    <tr>
      <td>Vendor: </td>
      <td><%= vendor.name %></td>
    </tr>
  </tbody>
</table>

<form action="/transactions/<%= @transaction.id %>/delete" method="POST">
  <input type="submit" value="Delete" />
</form>

<a href="/transactions/<%= @transaction.id %>/edit">Edit Transaction</a>
</div>
